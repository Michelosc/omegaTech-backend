{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "admin",
  "database": "OmegaDb",
  "entities": ["dist/**/*.model{.ts,.js}"],
  "migrations": ["database/migrations/**/*.ts"],
  "synchronize": true,
  "cli": {
    "migrationsDir": "database/migrations"
  }
}
